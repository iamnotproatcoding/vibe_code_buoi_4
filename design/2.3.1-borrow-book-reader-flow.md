# 2.3.1 - Mượn Sách (Độc Giả)

## Mô tả
Tính năng cho phép độc giả yêu cầu mượn sách từ hệ thống.

**Actor:** Độc giả  
**Yêu cầu:** Đăng nhập với vai trò độc giả

## Flowchart

```mermaid
flowchart TD
    Start([Độc giả xem chi tiết sách]) --> CheckAuth{Đã đăng nhập<br/>vai trò độc giả?}
    CheckAuth -->|Không| RedirectLogin[Chuyển hướng tới trang đăng nhập]
    RedirectLogin --> End1([Kết thúc])
    
    CheckAuth -->|Có| ClickBorrow[Click nút Mượn Sách]
    ClickBorrow --> CheckAvailable{Sách còn sẵn?<br/>số lượng > 0}
    
    CheckAvailable -->|Không| ErrorNotAvailable[Hiển thị lỗi:<br/>Sách không còn sẵn]
    ErrorNotAvailable --> End1
    
    CheckAvailable -->|Có| CheckMaxBorrows{Độc giả đã mượn<br/>>= 5 cuốn?}
    CheckMaxBorrows -->|Có| ErrorMaxBorrows[Hiển thị lỗi:<br/>Bạn đã mượn tối đa<br/>số sách cho phép]
    ErrorMaxBorrows --> End1
    
    CheckMaxBorrows -->|Không| CheckPendingBorrow{Đã có đơn mượn<br/>sách này chờ xác nhận?}
    CheckPendingBorrow -->|Có| ErrorPendingBorrow[Hiển thị lỗi:<br/>Đã có đơn mượn sách này<br/>đang chờ xác nhận]
    ErrorPendingBorrow --> End1
    
    CheckPendingBorrow -->|Không| CheckActiveBorrow{Đã mượn sách này<br/>chưa trả?}
    CheckActiveBorrow -->|Có| ErrorActiveBorrow[Hiển thị lỗi:<br/>Bạn đã mượn sách này<br/>và chưa trả]
    ErrorActiveBorrow --> End1
    
    CheckActiveBorrow -->|Không| CheckPenalty{Có khoản phạt<br/>chưa thanh toán?}
    CheckPenalty -->|Có| ErrorPenalty[Hiển thị lỗi:<br/>Vui lòng thanh toán<br/>các khoản phạt trước]
    ErrorPenalty --> End1
    
    CheckPenalty -->|Không| ShowBorrowForm[Hiển thị form mượn sách]
    ShowBorrowForm --> SelectDuration[Chọn thời hạn mượn:<br/>Mặc định 14 ngày<br/>Tối đa 30 ngày]
    SelectDuration --> ValidateDuration{Thời hạn hợp lệ?<br/>1-30 ngày}
    
    ValidateDuration -->|Không| ErrorDuration[Hiển thị lỗi:<br/>Thời hạn không hợp lệ]
    ErrorDuration --> SelectDuration
    
    ValidateDuration -->|Có| CreateBorrowRequest[Tạo đơn mượn:<br/>Trạng thái: Chờ xác nhận<br/>Thời hạn: Giá trị đã chọn]
    CreateBorrowRequest --> Success[Hiển thị thông báo:<br/>Yêu cầu mượn sách đã được gửi<br/>Vui lòng chờ xác nhận]
    Success --> RedirectHistory[Chuyển hướng tới<br/>Lịch sử mượn sách]
    RedirectHistory --> End([Kết thúc])
    
    style Start fill:#e1f5ff
    style End fill:#c8e6c9
    style End1 fill:#ffcdd2
    style ErrorNotAvailable fill:#ffcdd2
    style ErrorMaxBorrows fill:#ffcdd2
    style ErrorPendingBorrow fill:#ffcdd2
    style ErrorActiveBorrow fill:#ffcdd2
    style ErrorPenalty fill:#ffcdd2
    style ErrorDuration fill:#ffcdd2
    style Success fill:#c8e6c9
```

## Điều Kiện Mượn Sách
1. **Sách còn sẵn:** Số lượng sách có sẵn > 0
2. **Không vượt quá giới hạn:** Độc giả chưa mượn quá 5 cuốn sách
3. **Không có đơn mượn chờ xác nhận:** Chưa có đơn mượn sách này ở trạng thái "Chờ xác nhận"
4. **Chưa mượn sách này:** Chưa có đơn mượn sách này ở trạng thái "Đang mượn"
5. **Không có khoản phạt:** Không có khoản phạt chưa thanh toán

## Thời Hạn Mượn
- **Mặc định:** 14 ngày
- **Tối đa:** 30 ngày
- **Tối thiểu:** 1 ngày

## Edge Cases
- Sách không còn sẵn (số lượng = 0)
- Độc giả đã mượn quá 5 cuốn
- Độc giả đã mượn sách này trước đó (chưa trả)
- Độc giả có đơn mượn sách này đang chờ xác nhận
- Độc giả có khoản phạt chưa thanh toán
- Thời hạn mượn vượt quá 30 ngày
- Thời hạn mượn <= 0

